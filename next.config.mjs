/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['getstream.io', 'res.cloudinary.com', 'images.unsplash.com', 'unsplash.com'],
    },
};

export default nextConfig;
